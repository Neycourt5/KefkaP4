KefkaP4Trainer — WORK PLAN
Last updated: 2026-07-31 (session 4)
Baseline: main @ 335b544, version 0.1.9.0 (unreleased; v0.1.8.0 is the last tag)

====================================================================
CURRENT STATUS
====================================================================

The plugin builds clean (0 warnings, TreatWarningsAsErrors on) against
Dalamud 15.0.3 / API level 15, and 245/245 core tests pass.

Session 1 covered repository recovery, the Waju timeline/Flood parity audit
and the element-tell placement fix. Session 2 bumped the version to 0.1.9.0,
pushed session 1 to origin, and built the healer feature from the core
outward: virtual party health, the FF Logs damage dataset, the healer action
model/database/applier, and the plugin observer plus simulated party panel.

Session 3 reopened Flood as a visual-semantic mapping problem. Session 4 added
the deterministic co-healer and pinned the last five mechanics.

Still NOT started: audio expansion, the healer-action-id validation report,
native party-list and HUD debuff anchoring, and all multiplayer work.

Do not trust any claim below that is not marked DONE.

====================================================================
MILESTONES
====================================================================

[DONE] M0  Git recovery
       main == origin/main == fd0cc53 at session start, clean tree, zero
       untracked files, behind=0 ahead=0. Nothing needed preserving or
       integrating. This PC was already current.

[DONE] M1  Waju reference directory ignored
       Waju-Sims/ was ALREADY ignored (.gitignore:52) and had NEVER been
       tracked. No `git rm -r --cached` was required. Verified with
       `git log --all -- Waju-Sims` (empty) and `git check-ignore -v`.

[DONE] M2  Timeline parity audit
       All 50 method-track events in p4_main.tscn's p4_anim compared against
       KefkaP4Timeline.Events. Exact 1:1 match on time, order and integer
       argument. Pinned by WajuTimelineParityTests.

[DONE] M3  Flood of Naughts investigation
       No defect in the assignment, side selection, bot movement or grading —
       all four match p4_seq.gd exactly. The defect was presentational: the
       trainer never stated the resolved side. See WORK_PROGRESS.txt and the
       parity matrix in PORTING_NOTES.md for the full derivation.
       FloodBriefing now resolves it once and drives cue + failure text +
       debug panel from one value.

[DONE] M4  Element tell placement
       Lightning and ice badges now project from separate world anchors
       (default 4.2y and 1.2y above the arena floor) instead of sharing one
       ground anchor and stacking in screen space. Configurable heights plus
       optional horizontal spread.

[DONE] M5  Parity fixtures for every mechanic
       All mechanics are now Verified in the parity matrix. Chaos, Thrumming
       Thunder, Entropy, Blizzard Blowout and Mana Release were pinned in
       session 4; all nine fixtures passed first run, confirming rather than
       correcting. Two source quirks are asserted so they cannot be tidied
       away: inverted twister polarity between Entropy and Dynamic Fluid, and
       Mana Release flipping against the telegraph rather than the earlier
       resolution.

[DONE] M7  Virtual party health model
       Eight slots, HP/shields/multiplicative mitigation/simulation-time
       regens, deterministic damage pipeline with a full audit trail.
       36 tests. Pure core, no Dalamud.

[DONE] M9  Damage-event dataset + FF Logs calibration
       Nine events anchored to the KefkaP4Mitigation times. Reference figures
       recorded as PARTY TOTALS with the reasoning asserted by test; raw U:
       values preserved verbatim and visible in debug.

[DONE] M10 Healer action observation
       API 15 has no action/combat-event service. Implemented as cast-bar
       polling plus IsActionOffCooldown/GetCurrentCharges transitions.
       Instant GCDs are NOT identifiable (shared recast group) and this is
       stated in the UI. Action ids are unverified and cross-checked against
       the Lumina sheet at startup.

[PARTIAL] M8  Party-list overlay
       Standalone HealerWindow panel done (/kefkap4 healer). Anchoring it
       beside the NATIVE party list is not started.

[NOT STARTED] M6  Debuff-bar HUD anchoring
[DONE] M11 One-healer complementary co-healer
       WHM/AST get a barrier co-healer, SCH/SGE a pure one. Runs through the
       same applier a real press does; never touches HP or reduces boss damage.
       Standard covers ALTERNATE raidwides so the rest stay the player's - the
       first cut covered every one and carried the pull outright.
[NOT STARTED] M12 Countdown / targetable audio expansion
[NOT STARTED] M13 Multiplayer protocol + loopback
[NOT STARTED] M14 WebSocket relay
[NOT STARTED] M15 In-game verification of this session's changes

====================================================================
CURRENT BLOCKER
====================================================================

None technical. The blocker is verification: nothing from either session has
been loaded into FFXIV. Unit tests and a clean compile are the only evidence.

The highest-value single in-game action is reading the action-id cross-check
in /kefkap4 healer. HealerActionDatabase was written without a game to check
against, so several ids are probably wrong. The observer resolves every id
against the Lumina Action sheet at startup and lists disagreements in that
window, so one launch turns a guess into a correction list. Until that is
done, treat healer action recognition as unproven.

====================================================================
EXACT NEXT TASK
====================================================================

Work the in-game checklist in WORK_PROGRESS.txt section G, in order. Item [1]
(the action-id cross-check) gates the usefulness of everything else in the
healer feature.

If no game is available, the next implementable slices in dependency order:

  1. M12 audio: audit SoundCueService for duplicate playback across
     pause/resume/scrub, then add distinct countdown ticks, a pull cue and a
     separate targetable cue driven by the authoritative timeline event.
  2. A Lumina validation report for the healer action ids, mirroring what the
     status icon atlas already does - resolve each id's name from the Action
     sheet and list the disagreements.
  3. M6/M8 read-only native party-list and debuff-bar anchoring investigation.

Do NOT start multiplayer (M13/M14) yet. The virtual party and action-event
shapes are new and still likely to move; freezing them into a wire protocol
now would be premature.

====================================================================
RESUME HERE
====================================================================

Repo:        Documents\KefkaP4 (OneDrive - Monitor Technologies, LLC)
Branch:      main, pushed to origin/main
Version:     0.1.9.0 in the csproj; repo.json still says 0.1.8.0 ON PURPOSE
             (it must track the last RELEASED version; release.yml rewrites it
             from the tag). To release:
                 git tag v0.1.9.0 && git push origin v0.1.9.0
Layout:      FLAT. There is no DalamudPlugin/ folder in this repo.
             Waju-Sims/DalamudPlugin/ is a STALE 2026-07-30 copy. Never edit it.
Reference:   Waju-Sims/waju-sim/scenes/dmu/p4/{p4_seq.gd,p4_pos.gd,p4_main.tscn}

Build (needs Dalamud dev libs):
  export PATH="$LOCALAPPDATA/Microsoft/dotnet:$PATH"
  export DOTNET_ROOT="$LOCALAPPDATA/Microsoft/dotnet"
  export DALAMUD_HOME=<dir containing Dalamud.dll>
  dotnet test  KefkaP4Trainer.Core.Tests/KefkaP4Trainer.Core.Tests.csproj -c Release
  dotnet build KefkaP4Trainer/KefkaP4Trainer.csproj -c Release -p:Platform=x64

The .NET 10 SDK on this machine is NOT on PATH. It lives at
%LOCALAPPDATA%\Microsoft\dotnet. Without the two exports above, dotnet
resolves to the runtime-only install in Program Files and reports
"No .NET SDKs found". Dalamud dev libs are not at the XIVLauncher path either;
extract https://goatcorp.github.io/dalamud-distrib/latest.zip somewhere and
point DALAMUD_HOME at it.

Output DLL:  KefkaP4Trainer\bin\x64\Release\KefkaP4Trainer.dll
Package:     KefkaP4Trainer\bin\x64\Release\KefkaP4Trainer\latest.zip

Commands:    /kefkap4 [start|stop|pause|resume|reset|restart|config|debug|healer]
